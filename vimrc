colo desert                     " set color scheme
set number                      " enable line numbers
filetype plugin indent on       " loads filetype-specific indentation rules
set tabstop=4                   " represents <TAB> characters as spaces
set shiftwidth=4                " not sure, I'm just afraid to remove it
set softtabstop=4               " inserts spaces when I press <TAB> 
set expandtab                   " expands <TAB> into spaces
syntax on                       " enables syntax hilighting
set showmatch                   " higlights parenthesis/bracket pairs
